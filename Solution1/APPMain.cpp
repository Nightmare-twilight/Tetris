#include <stdio.h>
#include <string.h>

struct student
{
	int id;
	char name[256];
	int age;
};

int main(void)
{
	sturct student date;
	date, id = 1;
	strcpy(date, name, "山田太郎");
	date, age = 18;
	printf"学生番号：％d名前：年齢；％ｄ",
		date, id, date, name, date, age);
		return 0;
}